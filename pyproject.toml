[project]
name = "compliance-advisor"
version = "1.0.0"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = [
    "src",
    "src/functions",
]
addopts = "-ra -q --tb=short"
