[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = [
    "src",
    "src/functions",
]
addopts = "-ra -q --tb=short"

# Run tests with the project venv (Python 3.10+ required for X | Y union syntax):
#   .venv/Scripts/python -m pytest
